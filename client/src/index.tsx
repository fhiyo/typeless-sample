import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { App } from './components/App';

// 普通にtext-creative-generator作るのが良さそうだ。
// mockっぽいものを作れればそれが一番良さそう。
ReactDOM.render(<App />, document.getElementById('app'));
